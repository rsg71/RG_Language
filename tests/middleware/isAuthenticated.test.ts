
describe.skip('ensureAuthenticated', () => {

    it('should return an error if the user is not authenticated', () => {

    })

    it('should return next if the user is authenticated', () => {

    })
})

describe.skip('verifyIsTokenExpired', () => {

    it('should return true if the token is expired', () => {

        const now = Date.now();
        // const dateInPast = Date.now() - 10000;



    })

    it('should return false if the token is not expired', () => {

    })
})